- I want the check-in to actually be a menu that slides in from the right, using the
 ionic menu component. I should still have a save button, but maybe in the footer of the
 menu.
- Add a dashed grey line to "continue" start of time series if data doesn't fill whole
 span.
- When scanning, snap to the nearest day with the person's data, don't show blanks.
- Chart should update after a check-in
- Removing an item from check-in doesn't seem to save